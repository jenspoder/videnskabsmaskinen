{
  "name": "videnskabsmaskinen-backend",
  "version": "1.0.0",
  "main": "dist/handler.js",
  "type": "commonjs",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "clean": "rm -rf dist",
    "package": "npm run build && zip -r function.zip dist node_modules package.json -x '*.DS_Store'"
  },
  "dependencies": {
    "@aws-sdk/client-lambda": "^3.1037.0",
    "@aws-sdk/client-s3": "^3.700.0",
    "cheerio": "^1.0.0",
    "openai": "^4.0.0",
    "pdf2json": "^4.0.3"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  }
}
